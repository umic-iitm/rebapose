version: "3"
services:
  server:
    platform: linux/amd64
    container_name: pose-annotation-crowdsourcer-test
    image: hriddhi/pose-annotation-crowdsourcer-test
    build:
      dockerfile: Dockerfile
      context: .
    restart: always
    ports:
      - "80:8080"